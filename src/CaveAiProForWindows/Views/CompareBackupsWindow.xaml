<Window x:Class="CaveAiProForWindows.Views.CompareBackupsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compare backups"
        Height="720" Width="980"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Cave.Background}"
        Foreground="{DynamicResource Cave.Text}">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource Cave.TextMuted}" Margin="0,0,0,8"
                       Text="Pick two .json or .zip CaveAI files. Side-by-side diff: stations, symbols, field catalog, shots." />
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnPickA" Content="File A…" Padding="12,6" Margin="0,0,8,0" Click="BtnPickA_Click" />
                <TextBlock x:Name="TxtA" VerticalAlignment="Center" Text="(none)" TextTrimming="CharacterEllipsis" MaxWidth="320" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <Button x:Name="BtnPickB" Content="File B…" Padding="12,6" Margin="0,0,8,0" Click="BtnPickB_Click" />
                <TextBlock x:Name="TxtB" VerticalAlignment="Center" Text="(none)" TextTrimming="CharacterEllipsis" MaxWidth="320" />
            </StackPanel>
            <Button Content="Compare" Padding="14,6" Margin="0,12,0,0" HorizontalAlignment="Left"
                    Style="{StaticResource Cave.AccentButton}" Click="Compare_Click" />
        </StackPanel>
        <DataGrid x:Name="DiffGrid" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{DynamicResource Cave.Surface}" BorderBrush="{DynamicResource Cave.Border}"
                  Foreground="{DynamicResource Cave.Text}" HeadersVisibility="Column"
                  SelectionChanged="DiffGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Project" Binding="{Binding ProjectName}" Width="1.6*" />
                <DataGridTextColumn Header="St A" Binding="{Binding StationsA}" Width="48" />
                <DataGridTextColumn Header="St B" Binding="{Binding StationsB}" Width="48" />
                <DataGridTextColumn Header="St d" Binding="{Binding StationsDelta}" Width="48" />
                <DataGridTextColumn Header="Sym A" Binding="{Binding SymbolsA}" Width="52" />
                <DataGridTextColumn Header="Sym B" Binding="{Binding SymbolsB}" Width="52" />
                <DataGridTextColumn Header="Sym d" Binding="{Binding SymbolsDelta}" Width="52" />
                <DataGridTextColumn Header="Sh A" Binding="{Binding ShotsA}" Width="48" />
                <DataGridTextColumn Header="Sh B" Binding="{Binding ShotsB}" Width="48" />
                <DataGridTextColumn Header="Sh d" Binding="{Binding ShotsDelta}" Width="48" />
                <DataGridTextColumn Header="Cat A" Binding="{Binding CatalogA}" Width="52" />
                <DataGridTextColumn Header="Cat B" Binding="{Binding CatalogB}" Width="52" />
                <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="1.4*" />
            </DataGrid.Columns>
        </DataGrid>
        <Border DockPanel.Dock="Bottom" Margin="0,10,0,0" Padding="10" CornerRadius="6"
                BorderBrush="{DynamicResource Cave.Border}" BorderThickness="1"
                Background="{DynamicResource Cave.Surface}">
            <StackPanel>
                <TextBlock x:Name="PreviewTitle" Text="Plan preview (select a row)" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBlock x:Name="DiffSummaryText" TextWrapping="Wrap" Foreground="{DynamicResource Cave.TextMuted}" Margin="0,0,0,8" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,6,0">
                        <TextBlock Text="File A" Foreground="{DynamicResource Cave.TextMuted}" Margin="0,0,0,4" />
                        <Image x:Name="PreviewA" Height="180" Stretch="Uniform" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="6,0,0,0">
                        <TextBlock Text="File B" Foreground="{DynamicResource Cave.TextMuted}" Margin="0,0,0,4" />
                        <Image x:Name="PreviewB" Height="180" Stretch="Uniform" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </DockPanel>
</Window>
