<Window x:Class="CaveAiProForWindows.Views.ProjectCollaborationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Project collaboration"
        Width="640" Height="520"
        MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Cave.Background}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeaderText" TextWrapping="Wrap" Foreground="{DynamicResource Cave.TextMuted}" Margin="0,0,0,12" />
        <ListBox x:Name="CommentsList" Grid.Row="1" Margin="0,0,0,12"
                 DisplayMemberPath="DisplayLine" />
        <TextBox x:Name="CommentBox" Grid.Row="2" MinHeight="56" MaxHeight="120"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,8" Padding="8,6" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Refresh" Padding="12,6" Margin="0,0,8,0" Click="Refresh_Click" />
            <Button Content="Post comment" Padding="12,6" Margin="0,0,8,0" Click="Post_Click" />
            <Button Content="Close" Padding="12,6" IsCancel="True" Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
